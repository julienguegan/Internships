Nothing here yet

Unit test should hold some scripts (sce files) which test each component of the macros library.
Each component means including each function call, to assure it is there and to at least in a
limited way verify it works as expected.
